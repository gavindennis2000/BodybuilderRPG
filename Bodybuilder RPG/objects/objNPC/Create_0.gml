// objNPC create

if (TEST) { if (live_call()) {  // GMLive
    return live_result;
}}

// set animation to off
image_speed = 0;
image_index = 0;
faceStart = face;

// handle player interactions
touching = false;